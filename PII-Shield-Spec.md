# PII Shield — מסמך הוראה לבנייה עצמאית ע"י Claude Code

## 0. סיכום פרויקט (תקציר למי שקורא ראשון)

מערכת שמונעת דליפת מידע רגיש (PII) של חברות כשעובדים משתמשים בכלי AI חיצוניים
(ChatGPT, Claude, Gemini). המערכת מזהה מידע רגיש *ספציפי לחברה* (לקוחות, מספרי
תיק, סכומים) לפני שהוא נשלח לספק ה-AI, מחליפה אותו בטוקנים, ומחזירה את הערכים
האמיתיים בתשובה — כך שספק ה-AI אף פעם לא רואה את המידע המקורי.

שלושה רכיבים:
1. **Connector** — תוכנה שרצה בתוך הרשת של הלקוח, סורקת מקורות נתונים מוגדרים
   (CRM/DB), ושולחת רק טביעות אצבע (hashes) של ישויות רגישות לשרת המרכזי.
2. **Backend מרכזי** — multi-tenant, מנהל חברות/משתמשים, שומר אינדקסים, מגיש
   API לתוסף.
3. **תוסף דפדפן (Extension)** — רץ אצל כל עובד, מיירט טקסט לפני שליחה לכלי AI,
   מחליף בטוקנים, וממפה בחזרה בתשובה.

---

## 1. Stack טכני (החלטה סופית — לא לסטות ממנה בלי סיבה טובה)

| רכיב | טכנולוגיה | הערה |
|---|---|---|
| שפה | TypeScript בכל מקום | backend, connector, extension |
| Backend framework | NestJS | מובנה ל-multi-tenant, DI נקי |
| DB | PostgreSQL | + Prisma ORM |
| Auth | JWT + API keys לפי-חברה | admin console נפרד מ-extension |
| Extension | Chrome Extension Manifest V3 | React + Vite |
| Admin Console | React + Vite + Tailwind | SPA נפרד |
| Connector | Node.js CLI/daemon | רץ כ-Docker container אצל הלקוח |
| Hosting MVP | Render או Railway | Postgres מנוהל + web service |
| PII detection | regex + `compromise`/`wink-ner` לזיהוי ישויות בסיסי | לא לבנות NER מאפס |

**עקרון מנחה לקלוד קוד: אל תמציא ארכיטקטורה חדשה. עקוב אחרי הטבלה הזו בדיוק.**

---

## 2. מודל נתונים (schema ראשוני — Prisma)

```prisma
model Company {
  id            String   @id @default(cuid())
  name          String
  apiKey        String   @unique
  createdAt     DateTime @default(now())
  connectors    Connector[]
  employees     Employee[]
  entities      SensitiveEntity[]
  auditLogs     AuditLog[]
}

model Connector {
  id            String   @id @default(cuid())
  companyId     String
  company       Company  @relation(fields: [companyId], references: [id])
  sourceType    String   // "postgres" | "csv" | "salesforce" | "generic_api"
  lastSyncAt    DateTime?
  status        String   // "connected" | "syncing" | "error"
}

model Employee {
  id            String   @id @default(cuid())
  companyId     String
  company       Company  @relation(fields: [companyId], references: [id])
  email         String
  extensionKey  String   @unique
}

model SensitiveEntity {
  id            String   @id @default(cuid())
  companyId     String
  company       Company  @relation(fields: [companyId], references: [id])
  entityHash    String   // hash of normalized value, NOT the raw value
  entityType    String   // "name" | "id_number" | "case_number" | "amount" | "email" | "phone"
  confidence    Int      // 0-100, learned over time
  createdAt     DateTime @default(now())

  @@index([companyId, entityHash])
}

model AuditLog {
  id            String   @id @default(cuid())
  companyId     String
  company       Company  @relation(fields: [companyId], references: [id])
  employeeId    String
  eventType     String   // "blocked" | "allowed" | "user_override"
  entityType    String?
  createdAt     DateTime @default(now())
}
```

**חשוב:** לעולם לא לשמור `entityHash` בלי salt ייחודי לחברה. משתמשים ב-
`HMAC-SHA256(value.normalize(), companySalt)` כדי שאי אפשר יהיה לשחזר את הערך
המקורי משרת מרכזי גם אם הוא נפרץ.

---

## 3. תוכנית בנייה — 4 שבועות, לפי סדר תלות

### שבוע 1 — Backend + Data Model
- [ ] הקמת פרויקט NestJS, חיבור Postgres + Prisma, migration ראשוני לפי הסכימה למעלה
- [ ] Auth: יצירת חברה חדשה (admin only), הנפקת `apiKey`
- [ ] CRUD בסיסי: Company, Employee, Connector (endpoints + טסטים)
- [ ] Endpoint לקבלת `entityHash` batch מה-connector ושמירה ב-`SensitiveEntity`
- [ ] Endpoint שהתוסף קורא ממנו: "תן לי את כל ה-hashes הרלוונטיים לחברה שלי"
      (עם pagination/caching — יהיו אלפי רשומות)
- **הגדרת סיום שלב:** אפשר ליצור חברה ב-API, להזין hashes ידנית, ולקבל אותם
  בחזרה דרך endpoint אחר. טסט אינטגרציה שמוכיח את זה.

### שבוע 2 — Connector
- [ ] CLI/daemon ב-Node שמתחבר למקור נתונים אחד קודם (להתחיל מ-PostgreSQL
      גנרי / CSV upload, זה הכי קל להדגמה)
- [ ] לוגיקת extraction: לקרוא טבלת "לקוחות" לדוגמה (שם, ת.ז, מייל, טלפון),
      לנרמל כל ערך (lowercase, הסרת רווחים), לחשב hash, לשלוח ב-batch לbackend
- [ ] תזמון (cron) לסנכרון תקופתי + endpoint ידני "sync now"
- [ ] Docker container שאפשר להריץ עם `docker run` + קובץ config עם פרטי
      חיבור למקור הנתונים של הלקוח
- **הגדרת סיום שלב:** להריץ connector מול DB לדוגמה עם 50 "לקוחות" מדומים,
  לראות שה-hashes מגיעים ל-backend נכון.

### שבוע 3 — Extension (הליבה הקריטית)
- [ ] Manifest V3 extension, content script שרץ על chat.openai.com,
      claude.ai, gemini.google.com
- [ ] זיהוי regex parallel (ת.ז ישראלית: 9 ספרות + ביקורת ספרה, מייל, טלפון,
      IBAN, סכומי כסף)
- [ ] זיהוי NER בסיסי בעברית/אנגלית לשמות (להשתמש בספרייה קיימת, לא לאמן
      מודל)
- [ ] לוגיקת ההשוואה: לכל ישות שזוהתה — לחשב hash מקומי (**אותו** אלגוריתם
      hash+salt כמו ה-backend) ולבדוק מול הרשימה שהתקבלה מה-backend
- [ ] מנגנון טוקניזציה: `[NAME_1]`, `[ID_1]` וכו', עם מיפוי זמני בזיכרון (לא
      נשמר לדיסק, נמחק בסוף השיחה)
- [ ] Intercept של שליחת טקסט (לפני network request / לפני submit) —
      להחליף, לשלוח, ואז ליירט את התשובה שחוזרת ולהחזיר מיפוי
- [ ] UI קטן: badge שמראה "X פריטים הוסתרו בהודעה זו" + אפשרות ללחוץ ולראות
      מה הוסתר (ideally, admin permission-gated)
- **הגדרת סיום שלב:** להדביק טקסט עם "אבנר כהן, ת.ז 123456789" בצ'אט
  אמיתי, לראות שהטוקן נשלח בפועל (בדוק ב-Network tab), והתשובה חוזרת עם
  הערך האמיתי.

### שבוע 4 — Admin Console + Polish + Deploy
- [ ] React admin dashboard: יצירת חברה, ניהול connectors, צפייה ב-audit
      log, סטטיסטיקות (כמה חסימות החודש)
- [ ] הגדרת confidence threshold configurable לכל חברה
- [ ] Deploy: backend + DB ל-Render/Railway, extension ל-Chrome Web Store
      (unlisted/private לבדיקות ראשוניות)
- [ ] מסמך אבטחה קצר (מה נשמר, מה לא, איך ה-hashing עובד) — קריטי להראות
      ללקוחות פוטנציאליים
- [ ] README + הוראות התקנה ל-connector ולתוסף
- **הגדרת סיום שלב:** דמו מקצה לקצה, מוקלט וידאו: יצירת חברה → הרצת
  connector → התקנת extension → שימוש ב-ChatGPT עם מידע רגיש → הצגת ה-audit
  log בdashboard.

---

## 4. מה **לא** לבקש מקלוד קוד לבנות (ולמה)

- **NER מאומן מאפס** — להשתמש בספריות קיימות (`compromise`, `wink-nlp`,
  `natural`). אימון מודל משלך זה עבודת שבועות בפני עצמה.
- **אינטגרציות CRM אמיתיות (Salesforce API, HubSpot)** — לשלב 1 מספיק
  PostgreSQL/CSV גנרי. אינטגרציות אמיתיות = פרויקט נפרד לאחר ולידציה.
- **הסמכות ציות (SOC2)** — תהליך אנושי/עסקי, לא קוד.
- **תמיכה בכל ספקי ה-AI** — להתחיל מ-ChatGPT + Claude בלבד, זה כבר 90% מהשוק.

---

## 5. פרומפט פתיחה מוצע לקלוד קוד (להעתיק כמו שהוא)

```
אני בונה מוצר בשם PII Shield - מערכת שמונעת דליפת מידע רגיש ארגוני לכלי AI
כמו ChatGPT/Claude. המסמך המצורף (PII-Shield-Spec.md) מכיל את הארכיטקטורה,
מודל הנתונים, ותוכנית בנייה מפורטת ל-4 שבועות.

אני רוצה שתעבוד לפי התוכנית הזו בסדר: Backend קודם, אחר כך Connector, אחר
כך Extension, ולבסוף Admin Console. בכל שלב:
1. תבנה, תריץ טסטים, ותוודא שה"הגדרת סיום שלב" מתקיימת בפועל לפני שתעבור הלאה
2. תעדכן אותי מה נבנה ומה נשאר
3. אל תסטה מהstack הטכני שמוגדר בסעיף 1 בלי לשאול אותי קודם

תרוץ ברצף על כל הפרויקט מתחילתו ועד סופו, בלי לעצור ולחכות לאישור ביניים -
מ-Backend, דרך Connector, Extension, ועד Admin Console.

חשוב: תיצור ותעדכן קובץ BUILD_LOG.md לאורך כל הדרך (ראה סעיף 6.7) - מה
נבנה, אילו טסטים רצו, ואילו מקרי קצה מסעיף 6.6 טופלו בפועל. אם אתה נתקל
בסתירה בין שלבים, תתקן את השורש ותתעד את זה, ותמשיך הלאה. בסיום, תכתוב
סיכום סופי ב-BUILD_LOG.md עם מה עובד ומה נשאר לבדוק.
```

---

## 6.5 מפרט UX מפורט — Admin Console

### מסך: Onboarding לחברה חדשה (wizard, 4 צעדים)
1. **פרטי חברה** — שם, מייל admin, גודל (חופשי/עד עצוב אח"כ)
2. **חיבור מקור נתונים** — טופס עם dropdown לסוג המקור (Postgres/CSV/API),
   שדות חיבור (host/port/credentials מוצפנים), כפתור "בדוק חיבור" שנותן
   feedback מיידי (✓ מחובר / ✗ שגיאה + הסבר קונקרטי, לא "error occurred")
3. **הרצת סנכרון ראשוני** — progress bar + מונה חי ("נסרקו 340 רשומות...").
   **אם הסנכרון נכשל באמצע**: לשמור checkpoint ולאפשר "המשך מאיפה שנעצר",
   לא להתחיל מחדש
4. **הורדת התוסף + קוד חברה** — מסך סיום עם קוד חברה מודגש, כפתור העתקה,
   קישור ישיר ל-Chrome Web Store, והוראות טקסט פשוטות ל-3 צעדים

**מצב שגיאה שחייב טיפול:** אם admin סוגר את הדפדפן באמצע ה-wizard — לשמור
מצב (draft company) ולתת לחזור מאיפה שהפסיק, לא להתחיל הכל מחדש.

### מסך: ניהול עובדים (Employee Management)
- טבלה: שם, מייל, סטטוס (פעיל/לא הותקן עדיין/לא פעיל 30+ יום), תאריך הצטרפות
- **הוספת עובד בודד**: טופס מהיר, שולח מייל אוטומטי עם קישור התקנה + קוד
- **ייבוא המוני**: העלאת CSV (מייל בלבד), עם preview לפני אישור ודוח שגיאות
  ברור (שורה 14: מייל לא תקין) — לא לזרוק את כל הקובץ על שגיאה אחת
- **השבתת עובד**: כפתור אחד, מבטל את ה-extensionKey מיידית (עובד שעזב לא
  יכול להמשיך להשתמש בתוסף)
- **מצב "לא הותקן עדיין"**: תזכורת אוטומטית אחרי 3 ימים אם העובד לא הפעיל
  את התוסף

### מסך: Dashboard ראשי
- כרטיסי סיכום עליונים: סה"כ חסימות החודש, עובדים פעילים, connectors
  מחוברים (עם אינדיקטור ירוק/אדום לכל אחד)
- גרף: חסימות לאורך זמן (יומי, 30 יום אחרונים)
- טבלת audit log אחרונה: זמן, עובד, סוג ישות, פעולה — עם פילטר לפי עובד/סוג
- **מצב ריק (Empty State)**: אם עדיין אין נתונים — לא מסך ריק, אלא הנחיה
  ברורה "עדיין אין נתונים, ודא שהתוסף מותקן ופעיל" עם כפתור לבדיקת סטטוס

### מסך: הגדרות רגישות (Sensitivity Settings)
- Slider ל-confidence threshold (עם דוגמה חיה: "בסף הזה, X מהרשומות
  האחרונות היו נחסמות")
- טבלת סוגי ישויות (שם/ת.ז/מייל/סכום) עם toggle הפעלה/כיבוי לכל סוג בנפרד

---

## 6.6 מקרי קצה שחייבים טיפול מפורש (לא להשאיר לניחוש)

### בזיהוי/חסימה (Extension)
- **הודעה ארוכה מאוד (מסמך שלם מודבק)** — לא לקרוס, לעבד ב-chunks, להראות
  progress אם לוקח מעל שנייה
- **אותה ישות מופיעה כמה פעמים בהודעה** — כל המופעים מקבלים אותו טוקן
  (`[NAME_1]` בכל מקום), לא טוקן חדש לכל מופע
- **טקסט בעברית ואנגלית מעורב באותה הודעה** — הזיהוי חייב לעבוד על שניהם
  בו-זמנית, לא רק אחד
- **המשתמש עורך את ההודעה אחרי שהטוקניזציה כבר קרתה** (מוחק חלק, מוסיף טקסט)
  — למפות מחדש לפני שליחה, לא לסמוך על מיפוי ישן
- **backend לא זמין כשעובד רוצה לשלוח הודעה** — ברירת מחדל: **fail-safe**,
  כלומר לחסום הכל ברמת regex בלבד (בלי השוואה ל-DB של החברה) ולהראות
  למשתמש "לא ניתן לאמת מול רשימת החברה כרגע, הופעלה הגנה בסיסית בלבד" —
  **לעולם לא לשלוח בלי שום בדיקה כברירת מחדל**
- **מספר ת.ז שנראה כמו ת.ז אבל לא עומד בביקורת הספרה** — לא לחסום (false
  positive מיותר), אבל לתעד ברמת confidence נמוכה

### בסנכרון (Connector)
- **מקור הנתונים משתנה בין סנכרונים** (לקוח נמחק, שם עודכן) — הrun הבא
  צריך "לנקות" hashes ישנים שלא הופיעו יותר, לא רק להוסיף חדשים
- **חיבור נופל באמצע סנכרון גדול** — retry עם exponential backoff, לא
  להשאיר את הbackend עם דאטה חלקי בלי סימון "sync incomplete"
- **duplicate entities** (אותו לקוח מופיע פעמיים בטבלה עם רישיות שונה) —
  נירמול (lowercase, trim) לפני חישוב hash כדי שלא ייספרו כשתי ישויות

### כללי
- **חברה מוחקת את עצמה מהמערכת** — מה קורה לnested data (employees,
  entities, audit logs)? להגדיר: soft delete עם retention של 30 יום, לא
  מחיקה מיידית בלתי הפיכה
- **שני עובדים מנסים להתקין עם אותו extensionKey** (שיתפו קישור בטעות) —
  extensionKey ראשון-שמתחבר "תופס", השני מקבל הודעת שגיאה ברורה שמכוונת
  אותו לפנות ל-admin

---

## 6.7 הנחיה לקלוד קוד: ריצה רציפה עד סיום

קלוד קוד ירוץ ברצף על כל הפרויקט (סעיף 3, כל השלבים) ללא עצירה לאישור
בין שלבים. עם זאת, גם בריצה רציפה, הוא חייב:

1. אחרי כל שלב פנימי — להריץ את הטסטים האוטומטיים שנכתבו לאותו חלק, ולא
   להמשיך לבנות עליו אם טסט נכשל
2. לתעד בקובץ `BUILD_LOG.md` (ליצור בתחילת הריצה): מה נבנה בכל שלב, אילו
   טסטים רצו, ואילו מקרי קצה מסעיף 6.6 טופלו בפועל מול אילו נשארו לבדיקה
3. אם הוא נתקל בסתירה בין הנחות (למשל: מודל הנתונים לא תומך בפיצ'ר
   שהתבקש בשלב מאוחר יותר) — לתקן את השורש (המודל), לא להתקין "טלאי"
   מעל הבעיה, ולציין את התיקון ב-`BUILD_LOG.md`
4. בסיום כל הפרויקט — להריץ מעבר מלא: כל הטסטים, כל מקרי הקצה מ-6.6,
   ולסכם ב-`BUILD_LOG.md` תחת כותרת "סיכום סופי" מה עובד, מה לא נבדק,
   ומה ידוע כבעיה פתוחה

**קובץ ה-`BUILD_LOG.md` הוא הכלי שלך לבדוק בסוף מה קרה בפועל** — תקרא
אותו לפני שאתה סומך על המערכת ופורס אותה.

---

## 7. מה לעשות בזמן שקלוד קוד בונה (במקום לשבת ולחכות)

ריצה רציפה יכולה לקחת שעות עד ימים רצופים, תלוי בהיקף. זה הזמן הכי טוב
לקדם את הצד העסקי, לא רק לחכות:

1. **תפתח חשבון Railway** (railway.app) ותכיר את הממשק - project, service,
   environment variables, Postgres plugin. אין צורך לחבר כלום עדיין, רק
   להכיר לפני שהקוד מוכן
2. **תכין רשימת 5-10 משרדי רו"ח/עו"ד קטנים** שתרצה לדבר איתם בהמשך (סעיף
   6 המקורי) - שם, איש קשר, איך תגיע אליהם
3. **תבדוק את Chrome Web Store Developer** - הרשמה כמפתח (עולה תשלום
   חד-פעמי קטן) לוקחת זמן לאישור, כדאי להתחיל עכשיו כדי שזה מוכן כשהתוסף
   מוכן
4. **תחזור ותקרא את `BUILD_LOG.md`** מדי כמה שעות אם אתה יכול - לא כדי
   לעצור את קלוד קוד, אלא כדי לתפוס מוקדם אם משהו מתפתח בכיוון לא נכון

## 7.5 פריסה ב-Railway (אחרי שהבנייה הסתיימה)

כשקלוד קוד מסיים, לפני שאתה פורס:

1. **תקרא את `BUILD_LOG.md` מתחילתו ועד סופו**, במיוחד את "סיכום סופי" -
   זה המקום שבו כתוב מה לא נבדק
2. **תבקש מקלוד קוד עצמו להריץ בדיקת פריסה** - "תריץ build production
   מלא ותוודא שאין שגיאות, ותכין README עם כל משתני הסביבה הנדרשים
   ל-Railway"
3. **בReilway**: תיצור פרויקט חדש, תחבר את ה-GitHub repo (אם קלוד קוד
   דחף לרפו - כדאי לבקש ממנו ליצור אחד ב-git init + commits לאורך הדרך,
   לא רק בסוף), תוסיף Postgres plugin, תגדיר את משתני הסביבה מה-README
4. **תוסיף custom domain** רק אחרי שהכל עובד על ה-domain הזמני של Railway
5. **התוסף (Extension)** נפרס בנפרד - דרך Chrome Web Store Developer
   Dashboard, לא דרך Railway

**נקודה חשובה:** "מושלם" עדיין לא קיים בשלב הזה. הפריסה הראשונה היא כדי
שאתה תוכל להשתמש במערכת בעצמך ולבדוק אותה על נתונים אמיתיים - לא כדי
למכור אותה ללקוח הראשון באותו יום.

---

## 6. תזכורת אסטרטגית (לא לקלוד קוד — לך)

הבידול שקבענו: **עברית/ישויות ישראליות + כיסוי מעבר לצ'אט טקסט**. ברגע שהMVP
עובד, הצעדים הבאים הם *לא* קוד:
1. לדבר עם 3-5 משרדי רו"ח/עו"ד קטנים ולהראות דמו
2. לבדוק אם התמחור/הבידול באמת מדבר אליהם
3. רק אז להשקיע בהרחבת הכיסוי (Excel, סוכני AI גולשים, ספקים נוספים)

בלי שלב 1-2, גם המוצר הכי מהודר טכנית לא הופך ללקוחות משלמים.
