# Contact address used by every "request a demo" CTA on the landing page.
# Currently defaults to a temporary personal address (see App.tsx) until a
# real domain/mailbox is registered for the chosen name - override here or
# via a real deployment env var once that exists.
VITE_CONTACT_EMAIL="DA@101.ORG.IL"
