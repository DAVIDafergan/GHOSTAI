# Contact address used by every "request a demo" CTA on the landing page.
# Must be set to a real, monitored address before this page is shown to
# actual prospects - defaults to a placeholder otherwise (see BUILD_LOG.md).
VITE_CONTACT_EMAIL="contact@example.com"
